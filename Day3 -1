객체(Object) - 사물, 대상
	- 속성
	- 행위

ex) 자동차 공장(설계도 -> 생산)

클래스 -> 메모리 -> 객체
		  (실제                                                                                                    로 존재 - instance)
변수
함수

class 클래스명{
	멤버 변수
	메서드 
}

반환자료형 함수명(매개변수, ...){
	// 기능을 수행할 코드

	return 반환값;
}

스택 메모리 - 함수 전용 메모리

main() 	-> 모든 프로그램의 시작 함수
	-> JVM이 실행
